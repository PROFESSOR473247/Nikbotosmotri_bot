services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        fromGroup: telegram-bot-secrets
      - key: DATABASE_URL
        fromGroup: telegram-bot-secrets
